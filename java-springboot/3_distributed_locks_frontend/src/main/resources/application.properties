# Application name
spring.application.name=distributed-locks-frontend

# Server configuration
server.port=${SERVER_PORT:8082}

# Backend workshop API target
workshop.backend.url=${WORKSHOP_SESSION_BACKEND_URL:${WORKSHOP_BACKEND_URL:http://localhost:18082}}
workshop.source.path=${WORKSHOP_SESSION_WORKSPACE_PATH:${WORKSHOP_SOURCE_PATH:${WORKSHOP_BASE_PATH:}}}

# Logging
logging.level.com.redis.workshop.locks=INFO
