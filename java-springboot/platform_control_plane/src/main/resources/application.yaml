spring:
  application:
    name: platform-control-plane
  data:
    redis:
      host: ${WORKSHOP_HUB_REDIS_HOST:${SPRING_DATA_REDIS_HOST:localhost}}
      port: ${WORKSHOP_HUB_REDIS_PORT:${SPRING_DATA_REDIS_PORT:6379}}
      database: ${WORKSHOP_HUB_REDIS_DATABASE:${SPRING_DATA_REDIS_DATABASE:0}}
      username: ${WORKSHOP_HUB_REDIS_USERNAME:${SPRING_DATA_REDIS_USERNAME:}}
      password: ${WORKSHOP_HUB_REDIS_PASSWORD:${SPRING_DATA_REDIS_PASSWORD:}}
      ssl:
        enabled: ${WORKSHOP_HUB_REDIS_SSL_ENABLED:${SPRING_DATA_REDIS_SSL_ENABLED:false}}

platform:
  controlplane:
    release:
      environment: local
    session:
      defaults:
        ttl: ${PLATFORM_CONTROLPLANE_SESSION_DEFAULTS_TTL:180m}
    portal:
      session:
        cookie-name: ${PLATFORM_CONTROLPLANE_PORTAL_SESSION_COOKIE_NAME:portal_session}
        ttl: ${PLATFORM_CONTROLPLANE_PORTAL_SESSION_TTL:180m}
        secure-cookie: ${PLATFORM_CONTROLPLANE_PORTAL_SESSION_COOKIE_SECURE:false}
        redis-key-prefix: ${PLATFORM_CONTROLPLANE_PORTAL_SESSION_REDIS_KEY_PREFIX:platform:portal:sessions:}
        marketing-preference-redis-key-prefix: ${PLATFORM_CONTROLPLANE_PORTAL_MARKETING_PREFERENCE_REDIS_KEY_PREFIX:platform:portal:marketing_preferences:}
    execution:
      base-url: ${EXECUTION_PLANE_BASE_URL:http://localhost:9001}
      shared-secret: ${EXECUTION_PLANE_SHARED_SECRET:}
      launch-timeout: 450s
      restart-timeout: 450s
      readiness-timeout: 480s
      readiness-poll-interval: ${PLATFORM_CONTROLPLANE_EXECUTION_READINESS_POLL_INTERVAL:15s}
      termination-timeout: 30s
    runtime:
      public-base-url:
      provisioning-delay: 250ms
      initialization-delay: 250ms
      termination-delay: 100ms
