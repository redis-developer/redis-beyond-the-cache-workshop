# Application name
spring.application.name=session-management-frontend

# Server configuration
server.port=${SERVER_PORT:8080}

# Backend workshop API target
workshop.backend.url=${WORKSHOP_BACKEND_URL:http://127.0.0.1:18080}
workshop.source.path=${WORKSHOP_SOURCE_PATH:${WORKSHOP_BASE_PATH:}}

# Logging
logging.level.com.redis.workshop.session=INFO
