# Application name
spring.application.name=full-text-search-frontend

# Server configuration
server.port=${SERVER_PORT:8081}

# Backend workshop API target
workshop.backend.url=${WORKSHOP_BACKEND_URL:http://127.0.0.1:18081}
workshop.source.path=${WORKSHOP_SOURCE_PATH:${WORKSHOP_BASE_PATH:}}

# Logging
logging.level.com.redis.workshop.search=INFO
