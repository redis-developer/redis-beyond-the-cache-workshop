---
id: R2
title: Scaffold validator numbered views
status: done
depends_on: ["P8"]
owner: codex
allowed_files: ["scripts/validate-workshops.sh"]
forbidden_files: ["scripts/new-workshop.sh", "java-springboot/**", "workshop-frontend-shared/**", "plans/embedded_vscode_editor/P8_content_scaffold_copy.md"]
---

# Goal

Align the scaffold smoke validator with the numbered content files generated by `scripts/new-workshop.sh`.

# Why This Exists

The scaffold now generates `views/0.yaml` and `views/1.yaml`, but the smoke validator still expects old service-named content files. This keeps `bash scripts/validate-workshops.sh` failing even though the scaffold output is correct.

# Required Changes

1. Update the smoke validator expected files from service-named views to numbered views.
2. Update expected content view metadata from service-named view ids to `0` and `1`.
3. Update assertion targets to point at the numbered files.

# Acceptance Criteria

1. The validator no longer expects `validation-smoke-home.yaml` or `validation-smoke-editor.yaml`.
2. The validator checks `views/0.yaml` and `views/1.yaml`.

# Verification

1. `bash scripts/validate-workshops.sh`

# Out Of Scope

1. Do not edit the scaffold generator.
2. Do not edit workshop content.
